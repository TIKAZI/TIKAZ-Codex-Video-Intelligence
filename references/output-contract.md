# Output contract

Each source record must include `source_id`, `platform`, `canonical_url`, `access_status`, `transcript_status`, `visual_status`, `evidence_level`, `claims`, `timestamps`, `uncertainties`, and `next_action`.

Cross-source output must include agreements, conflicts, complementary evidence, missing evidence, confidence, and a recommended decision or learning sequence.
